template <typename T>
T Suma(T a, T b) {
    return a + b;
}

template <typename T>
T Resta(T a, T b) {
    return a - b;
}

template <typename T>
T Multiplicacion(T a, T b) {
    return a * b;
}

template <typename T>
T Division(T a, T b) {
    return a / b;
}